{
  "name": "bly-mortgage-calculator-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "AI address-estimator proxy (Cloudflare Worker) for the Bly Team Mortgage Calculator",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
